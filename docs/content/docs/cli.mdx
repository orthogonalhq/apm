---
title: CLI Reference
description: Reference documentation for OPM CLI commands
---

## Commands

### `opm install`

Install a skill package.

```bash
opm install <package-name>
```

### `opm publish`

Publish a skill package to the registry.

```bash
opm publish
```

### `opm search`

Search for skill packages in the registry.

```bash
opm search <query>
```
